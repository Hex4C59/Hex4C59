+++
title = "0. 中文题名"
date = {{ .Date }}
draft = true
author = "Hex4C59"
description = "LeetCode 0 中文题名题解，包含题意概述、思路分析、复杂度与 Python / Rust 实现。"
summary = "LeetCode 0 中文题名题解，包含暴力思路、优化思路、复杂度分析，以及 Python / Rust 实现。"
tags = ["leetcode"]
categories = ["LeetCode"]
series = []
difficulty = "medium"
problem_id = 0
source = "leetcode"
languages = ["Python", "Rust"]
ShowToc = true
+++

> 新建文章后，请优先先改 3 个 front matter 字段：
>
> 1. `title`：统一写成 `题号. 中文题名`
> 2. `description`：统一写成 `LeetCode 题号 中文题名题解，...`
> 3. `summary`：统一写成 `LeetCode 题号 中文题名题解，...`
>
> 建议不要再混用英文题名，除非你明确想保留官方英文名作为正文补充说明。

## 题目链接

- 

## 题目描述

> 用 2~4 句话概括题意。
>
> 建议写清楚：输入是什么、要求输出什么、有没有固定长度 / 唯一答案 / 子数组或子串等关键约束。

## 示例

### 示例 1

```text
输入：
输出：
解释：
```

### 示例 2

```text
输入：
输出：
解释：
```

### 示例 3

```text
输入：
输出：
解释：
```

## 提示

- 
- 
- 

## 思路分析

### 暴力法

先写最直接的思路，说明为什么它容易想到。

如果题目适合枚举，就写清楚：

- 枚举的对象是什么
- 每次枚举时统计什么
- 为什么会有重复计算

如果方便，建议补一个小的手推例子：

```text
示例输入：

过程拆解：
...
```

然后明确指出复杂度问题：

- 时间复杂度是多少
- 为什么这个复杂度在数据范围下不够好

> 如果你实际提交后发现暴力解超时，可以保留下面这种说明：
>
> 根据我实际提交的结果，暴力解会超时。
>
> 因此下面的暴力代码只适合理解题意和对比思路，不适合作为最终提交方案。

### 优化思路

这里写真正推荐的解法，例如：

- 哈希表
- 双指针
- 定长滑动窗口
- 不定长滑动窗口
- 动态规划
- 二分
- 单调栈
- 贪心

建议按下面这个顺序展开：

1. 先说为什么能优化
2. 再说优化后的核心状态是什么
3. 最后说明每一步如何更新答案

如果是滑动窗口 / 双指针类题，建议明确写出：

- 谁进入窗口
- 谁离开窗口
- 哪个变量维护窗口状态
- 哪个变量维护最终答案

如果方便，也建议补一个手推过程：

```text
示例输入：

窗口 / 指针变化过程：
...
```

最后收束到结论：

- 每个元素被处理几次
- 总时间复杂度为什么能降到 `O(n)` 或其他更优复杂度

## 通用解法模板

> 总结这道题背后的可复用模式，而不是只总结这道题本身。

可以参考下面这个写法：

1. 先初始化 / 建立初始状态
2. 再移动指针 / 滑动窗口 / 逐步转移状态
3. 在过程中维护答案
4. 最后返回结果

如果这是某个经典题型，建议直接点明：

- 这是哈希表的“边查边存”模板
- 这是定长滑窗模板
- 这是不定长滑窗模板
- 这是 DP 的“定义状态 + 转移”模板

## 代码实现

### 暴力法 Python

```python
class Solution:
    def solve(self, ...):
        # 这里写最直接的解法
        pass
```

### 暴力法 Rust

```rust
impl Solution {
    pub fn solve(/* 参数 */) -> /* 返回值 */ {
        // 这里写最直接的解法
        todo!()
    }
}
```

### 优化解法 Python

```python
class Solution:
    def solve(self, ...):
        # 这里写最终推荐提交的解法
        pass
```

### 优化解法 Rust

```rust
impl Solution {
    pub fn solve(/* 参数 */) -> /* 返回值 */ {
        // 这里写最终推荐提交的解法
        todo!()
    }
}
```

## 复杂度分析

| 项目 | 复杂度 | 说明 |
| --- | --- | --- |
| 暴力法时间复杂度 |  |  |
| 优化解法时间复杂度 |  |  |
| 空间复杂度 |  |  |

如果暴力法会超时，建议补一句：

把最坏情况代入后，暴力法需要处理的操作次数过多，无法在题目的时间限制内通过，因此最终应使用优化解法。

## 易错点提醒

### 1. 

写一个最容易踩坑的点，例如：

- 先查再存，还是先存再查
- 窗口初始化是否完整
- 左右指针移动顺序是否正确
- Rust 中下标、引用、所有权、类型转换是否容易出错

### 2. 

再补一个实现层面的坑。

### 3. 

如果题目适合 Rust 经验总结，也可以单独写一条语言相关提醒。

## 小结

| 方法 | 时间复杂度 | 空间复杂度 | 是否推荐提交 |
| --- | --- | --- | --- |
| 暴力法 |  |  |  |
| 优化解法 |  |  |  |

最后一段建议写成：

- 这题属于什么经典类型
- 真正需要记住的模式是什么
- 下次遇到类似题，应该优先想到什么
